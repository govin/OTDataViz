using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace OpenTableDataVizService
{
	public class ReservationFeedProcessor
	{
		public void Process()
		{
			// get the base url from config file.

			// construct the feed url.

			// make a request to feed url and map the response to a C# class.

			// get the prevMaxResIdStoredInMongo stored in reservation collection in mongo
			
			//var resoData = FetchData(url);
			//if (resoData.max_resid > prevMaxResId && resoData.max_resid > prevMaxResIdStoredInMongo)
			//{
			//	FetchData(resoData.href_prev);
			//}

			// make a request to the 
		}
	}
}